Note: This program generate two sequences randomly. Please specify your own sequences for different uses.
      At the current stage, the program only supports two sequences with the same lengh, which can be divided by 16. 

Use the script titled run as a usage guide.

Use sweep_params.sh to recreate the data for the design space search.

Use the file Makefile to build the non-SVM version of the application.

Use the file Makefile.svm to build the SVM version of the application.
